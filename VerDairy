版本日记
========

v0.0
----
- 任务 ID：`Xytheta-Transa-v0.0`。
- 当前初始基线版本：三辆带 LiDAR 的平面小车，在带墙和简单障碍物的场地中运动。
- 奖励函数以“每辆车 LiDAR 当前新增覆盖的未探索面积”为主。
- 每辆车维护独立的已探索地图，A 车探索过的区域不会算到 B 车头上。
- 加入靠近边界的惩罚，鼓励小车不要贴墙。
- 加入出界终止惩罚。
- `random_agent.py` 支持 `--reward_guided`，可以运行一个简单的贪婪探索控制器。
- `random_agent.py` 支持探索面积 CSV 记录；绘图和实验输出放在 `logs/` 下，不提交到 GitHub。
- 后续版本中该脚本已改名为 `exploration_agent.py`，`random_agent.py` 仅作为旧命令兼容入口保留。

v0.1-greedy-recovery
--------------------
- 任务 ID 仍然是：`Xytheta-Transa-v0.0`。
- Git tag：`v0.1-greedy-recovery`。
- 这一版重点修复“简单的贪婪算法探索策略”在障碍物附近容易卡住、刚体小车容易翻车的问题。
- 在 `random_agent.py` 的 `RewardGuidedExplorer` 中加入堵塞检测：
  - `blocked_steps` 记录某辆车连续被前方障碍挡住的 step 数。
  - `recovery_steps` 记录当前是否处于倒车脱困状态。
  - `recovery_turn` 记录倒车时应该向哪边转。
- 贪婪策略的安全逻辑更新为：
  - 前方 LiDAR 距离小于 `0.65 m` 时，停止前进并向更空的一侧转向。
  - 前方 LiDAR 距离小于 `0.4 m`，或连续堵塞超过 10 个 step 时，进入倒车脱困。
  - 倒车脱困持续 18 个 step，动作约为“倒车 + 转向”。
- 在 `mdp/actions.py` 中加入平面车辆约束：
  - 每次给刚体小车写速度前，把姿态投影回 `roll=0, pitch=0, yaw=当前 yaw`。
  - 同时把 z 高度恢复到初始高度，避免碰撞力矩把车掀翻。
- 在 `random_agent.py` 中，`env.step()` 后、GUI 同步前也会把三辆车投影回平面，保证画面中的车不会显示为翻倒状态。
- 已用 headless 方式完成随机策略和贪婪策略各 600 秒仿真时间的对比实验：
  - 随机策略 CSV：`logs/exploration/strategy_compare_20260513_011227/random_10min.csv`
  - 贪婪策略 CSV：`logs/exploration/strategy_compare_20260513_011227/greedy_10min.csv`
  - 对比图：`logs/exploration/strategy_compare_20260513_011227/random_vs_greedy_episode_relative.png`
  - 这些实验输出位于 `logs/`，用于本地分析，不提交到 GitHub。
- 在这次 600 秒对比中，按 episode 内相对时间统计三辆车总已探索面积，贪婪策略明显高于随机策略：
  - 5.0 秒：随机约 `21.025 m^2`，贪婪约 `23.441 m^2`。
  - 20.0 秒：随机约 `34.176 m^2`，贪婪约 `43.054 m^2`。
  - 44.5 秒：随机约 `50.370 m^2`，贪婪约 `73.240 m^2`。

v0.2-offroad-terrain
--------------------
- 新增任务 ID：`Xytheta-Transa-Offroad-v0.0`。
- 保留旧任务 ID：`Xytheta-Transa-v0.0`，作为平面基线环境，不直接覆盖。
- 新增固定 15m x 15m 越野非结构化地形：
  - 地图边界为 `[-7.5, 7.5] x [-7.5, 7.5]`。
  - 使用 Isaac Lab `TerrainImporterCfg + TerrainGeneratorCfg` 生成。
  - 固定地形 seed 为 `20260515`，保证每次运行地形一致。
  - 地形包含随机起伏、块状乱石、离散障碍、锥形障碍和低台阶。
- 新增项目内四轮差速车 URDF 资产：
  - 文件：`source/xytheta_transa/xytheta_transa/assets/offroad_diff_drive_car.urdf`。
  - 车辆是 articulation，不再是被直接写平面速度的简单刚体。
  - 车辆保留重力、roll、pitch、轮地接触、打滑和翻车等物理现象。
- 新增 `DifferentialWheelVelocityAction`：
  - 输入仍然是 `[forward_velocity, yaw_rate]`。
  - 内部根据轮半径和轮距换算左右轮速度目标。
- Offroad 环境改为单车 LiDAR：
  - LiDAR 为 16 channels。
  - 垂直视场为 `[-15°, 15°]`。
  - 水平视场为 360°。
  - 最大扫描距离为 `10m`。
  - LiDAR 目标包含 `/World/ground` 和 offroad 静态障碍。
- `random_agent.py` 不再硬编码三辆车：
  - 会从环境配置中读取 `car_names`、`lidar_names`、`arena_bounds` 和 `lidar_max_distance`。
  - 平面刚体车仍然会执行旧的平面投影，保持 v0.0 稳定。
  - articulation 越野车不会执行平面投影，因此可以真实倾斜、颠簸和翻车。
- 主运行脚本改名为 `exploration_agent.py`：
  - 原因是它已经不只是随机策略脚本，而是统一负责随机探索、贪婪探索、demo motion 和 CSV 记录。
  - 新推荐命令使用 `python scripts/exploration_agent.py ... --greedy`。
  - `random_agent.py` 暂时保留为兼容入口，旧命令仍然可以转发运行。
- CSV 记录在 offroad 环境下只记录单车 `car_id=0` 的已探索面积。
- 当前 offroad 评价指标仍然只看单车已探索面积随时间变化，暂时不记录完整训练数据字段。
